import type { NextConfig } from "next";

// Revisando configuración personalizada para Next.js y Turbopack

const nextConfig: NextConfig = {
  /* config options here */
  reactCompiler: true,
};

export default nextConfig;
